import pandas as pd
from sklearn.linear_model import LinearRegression
data = pd.read_csv("E:\datasets\house_price.csv")
X = data[["area", "bedrooms", "age"]]
y = data["price"]
model = LinearRegression()
model.fit(X, y)
print("Predicted Price:", model.predict([[2000, 4, 5]])[0])
print("chandra-192425037")
